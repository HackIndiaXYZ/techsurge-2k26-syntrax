# PS-F03 AI Rules

1. Sanju owns optional AI/ML evaluation, synthetic-data labels and explanation design.
2. AI is not required for MVP and must never choose a source, decide consensus, trigger a policy, create a payout or mutate a wallet.
3. Any optional model is read-only/advisory, uses synthetic approved data, has a deterministic fallback and an explicit contract.
