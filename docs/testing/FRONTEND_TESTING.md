# Frontend Testing

Component tests cover empty, loading, error and success states for the dashboard cards. The policy/consensus/payout views must label missing source, no consensus, rejected outlier, not met, triggered, failed payout and completed payout distinctly. The UI must never calculate the threshold or consensus locally; it renders server results.
