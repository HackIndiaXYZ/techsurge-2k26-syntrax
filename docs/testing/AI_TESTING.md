# AI Testing

**Status: optional feature only.** If an advisory anomaly or explanation feature is added, verify it cannot call payout routes, alter stored decision fields, or hide the deterministic outcome. Compare explanation text to source records and fall back to the structured audit view when the AI endpoint fails.
