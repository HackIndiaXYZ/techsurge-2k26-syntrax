# AI Integration Contract

> **STATUS: FINAL (MVP)**  
> **OWNER:** Sanju (AI/ML)  
> **CONSUMERS:** Ramraj (Backend), Nikhil (Frontend)

## 1. Architectural Boundary

The AI layer is **advisory only**. It sits *on top* of the deterministic backend pipeline.
It does not:
- Make financial decisions
- Approve/reject payouts
- Override consensus
- Evaluate weather policies

It **only** consumes structured, deterministic facts generated by the backend and provides:
1. Advisory anomaly detection (MAD modified z-score, no LLM)
2. Human-readable event explanations (English, Hindi, Telugu)
3. Local-language settlement notifications (Post-settlement only)

---

## 2. Integration: Backend to AI

The AI module exposes a unified endpoint `/v1/ai/event` that the FastAPI backend can call, or directly import `process_backend_event` if running in the same process.

The backend must provide facts using the **Canonical Event Contract**:

```json
{
  "event_id": "evt_12345",
  "policy_id": "SYN-POLICY-RAIN-001",
  "timestamp": "2026-09-18T10:00:00Z",
  "source_observations": [
    {
      "source_id": "SYN-SRC-A",
      "source_code": "SYN-SRC-A",
      "value": 103.0,
      "unit": "mm",
      "observed_at": "2026-09-18T10:00:00Z"
    }
  ],
  "validated_observations": [
    {
      "source_id": "SYN-SRC-A",
      "source_code": "SYN-SRC-A",
      "value": 103.0,
      "unit": "mm",
      "validation_state": "ACCEPTED"
    }
  ],
  "consensus_value": 102.0,
  "consensus_status": "ACHIEVED",
  "outlier_sources": [],
  "trigger_status": "TRIGGERED",
  "trigger_reason": "THRESHOLD_MET",
  "payout_amount_paise": 1000000,
  "settlement_status": "COMPLETED",
  "idempotency_status": "SETTLED"
}
```

*Note: All states correspond directly to the deterministic backend architecture. The AI layer does not guess or interpolate.*

---

## 3. Integration: AI to Frontend

The unified `/v1/ai/event` endpoint returns a `FrontendAIResponse` designed for Nikhil's UI. It guarantees safety bounds and provides immediate translation.

```json
{
  "event_id": "evt_12345",
  "is_advisory": true,
  "is_synthetic": true,
  "fallback_used": false,
  "basis_risk_notice": "The prototype settles against a predefined weather index...",
  "anomaly": {
    "source_scores": [
      {
        "source_code": "SYN-SRC-A",
        "value": 103.0,
        "anomaly_level": "NORMAL",
        "explanation": "Source SYN-SRC-A reported 103.0mm, which is within normal range..."
      }
    ]
  },
  "explanation_en": {
    "title": "Synthetic Payout Completed",
    "body": "Policyholder Ravi Kumar in Kurnool Synthetic Micro-Region received a synthetic settlement..."
  },
  "explanation_hi": { ... },
  "explanation_te": { ... },
  "notification_en": {
    "message": "Dear Ravi Kumar, your parametric rainfall insurance policy for Kurnool Synthetic Micro-Region has been triggered..."
  },
  "notification_hi": { ... },
  "notification_te": { ... }
}
```

### Safety Flags (Frontend MUST display)
- **`is_advisory`**: AI interpretation is not the financial record.
- **`basis_risk_notice`**: Mandatory disclaimer that actual damage is not assessed.
- **`is_synthetic`**: Confirms no real money is exchanged.

---

## 4. How to Mount (Backend)

In `main.py` (Ramraj):

```python
from fastapi import FastAPI
from ai.router import router as ai_router

app = FastAPI()

# Mount the AI advisory endpoints
app.include_router(ai_router)
```

## 5. Demo Scenarios Verified

The integration is proven against all 10 canonical architecture scenarios:
1. `clean-trigger`
2. `corrupted-source`
3. `corrupted-source-outlier`
4. `no-consensus`
5. `duplicate-event`
6. `missing-source`
7. `payout-retry`
8. `below-threshold`
9. `extreme-invalid`
10. `barely-crossed`

All scenarios produce deterministic, language-localized explanations without hallucinations, enforcing the financial authority of the backend.
