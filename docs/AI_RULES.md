# AI Rules

Compatibility entry point. PS-F03 AI boundaries are defined in [AI Agent Rules](ai/AI_AGENT_RULES.md): AI may support offline analysis or explanation, but it must never decide triggers, create payouts, or mutate wallet state.
