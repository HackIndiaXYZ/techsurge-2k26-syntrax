# Vercel

Vercel is the selected frontend host if deployment is used. It hosts the Next.js dashboard and public API base URL configuration only. It does not host database secrets, payout authority, or the settlement service. See [Vercel Deployment](../deployment/VERCEL_DEPLOYMENT.md).
