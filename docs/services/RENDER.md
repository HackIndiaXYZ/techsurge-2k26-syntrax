# Render

Render is the selected FastAPI host if deployment is used. It holds server-only environment configuration and serves the deterministic API. It is not evidence of multi-region resilience or production SLA. See [Render Deployment](../deployment/RENDER_DEPLOYMENT.md).
