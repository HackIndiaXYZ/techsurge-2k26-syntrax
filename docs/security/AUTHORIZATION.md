# Authorization

The browser receives read projections and may invoke only documented simulation actions with the demo operator credential. Policy modification, payout execution and database access are server-side service responsibilities. No API accepts client-provided payout amount, policy status, consensus result, wallet balance, or audit timestamp.
