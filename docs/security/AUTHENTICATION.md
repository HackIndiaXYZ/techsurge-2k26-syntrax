# Authentication

**Prototype decision:** use a simple per-environment demo operator token for protected simulation/configuration routes, stored server-side. It is not a policyholder login system. A real product would require user authentication, operator identity, MFA for sensitive actions, session management and revocation.
