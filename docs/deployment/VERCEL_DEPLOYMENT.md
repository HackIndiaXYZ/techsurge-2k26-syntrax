# Vercel Frontend Deployment

Deploy the Next.js frontend after setting the server API base URL. Restrict public configuration to non-secret values such as `NEXT_PUBLIC_API_BASE_URL`. Verify the synthetic banner, polling behavior, empty/error UI, and CORS against the deployed backend. Do not put database keys or payout credentials in browser variables.
